// A card that shows a treasure chest upon check-in, can only claim once per event

type Props = {
    isCheckedIn: boolean,
    onClaim: () => void
    canClaim: boolean
};

onClaim

export default function TreasureCard({isCheckedIn, onClaim, canClaim}: Props) {
    


    return(<></>);
}